# Install in a Kaggle GPU notebook, then restart its Python kernel.
# Keep the CUDA-compatible torch supplied by Kaggle when it satisfies this range.
# This pins a deliberate Transformers 4.x/PEFT API contract; it is not a latest-version claim.
-r requirements.txt
torch>=2.4,<3
transformers==4.48.3
tokenizers==0.21.0
peft==0.14.0
accelerate==1.3.0
bitsandbytes>=0.45.2
datasets==3.2.0
safetensors>=0.4.5,<1
