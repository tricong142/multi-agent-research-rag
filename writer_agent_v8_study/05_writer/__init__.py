"""Writer agent, Plan A: bounded local revision without graph routing.

Load with importlib.import_module('05_writer.node') because the directory name
starts with a digit. All internal imports are relative to avoid sibling agents'
config.py and schemas.py colliding in sys.modules.
"""
